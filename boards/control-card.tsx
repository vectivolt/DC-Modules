// Control card — one card, one MCU, for every module in the 30 / 40 / 50 kW family.
import { ControlCard } from "../packages/common-components/boards";
export default () => <ControlCard w={120} h={80} />;
